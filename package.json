{
  "name": "heatmap",
  "version": "1.0.0",
  "description": "heatmap.js is a plugin for canvas chart",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/rollup src/heatmap.js --format umd --name 'heatmap'  --output dist/heatmap.js",
    "start": "npm run build && node ./build/build.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tm-roamer/heatmap.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tm-roamer/heatmap/issues"
  },
  "homepage": "https://github.com/tm-roamer/heatmap#readme",
  "devDependencies": {
    "rollup": "^0.41.6"
  }
}
